import KinouGaiyou from 'components/KinouGaiyou';

# Spiral

You can create a spiral.

<KinouGaiyou shortcut='' command='Spiral' button='Toolbar' />

1. Click the icon on the toolbar.
2. Click at the starting point of the spiral axis.
   - `X` : **X axis** : Constrains to the X-axis.
   - `Y` : **Y axis** : Constrains to the Y-axis.
   - `Z` : **Z axis** : Constrains to the Z-axis.
3. Move the cursor and click at the ending point of the spiral axis.
4. Move the cursor and click at the position for the spiral diameter.
5. Set the options in the command dialog.
   - **Turns** : Specifies the number of rotations.
   - **Radius** : Specifies the diameter.
   - **Angle OR Pitch** : Specifies the angle or pitch. Each value is linked.
   - **Handedness** : Reverses the direction of rotation.
   - `A` : **Angle** : Specifies the angle with mouse movement.
   - `D` : **Length** : Specifies the length of the axis with mouse movement.
   - `R` : **Radius** : Specifies the diameter with mouse movement.
6. Confirm with **OK** in the command dialog or right-click.
7. A spiral is created.