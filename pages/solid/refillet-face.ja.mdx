import VideoPlayer from '/components/VideoPlayer';
import Reference from '/components/Reference'
import Kst from '/components/KeyboardShortcutsTitle'

# Refillet Face（フィレットをやり直す）

<Reference shortcut="" command="Refillet Face" button="" />

ソリッドやシートの個々のフィレット面を選択せずにフィレットを再実行できます。距離は調整可能です。

## 基本的な使い方

1. フィレットをやり直したい面を含むすべての面を選択します。
2. コマンドパレットで**Refillet Face**を実行します。
3. 黄色い点を移動してフィレットの半径を指定します。
4. キーボードショートカットでオプションを設定します。
    - <Kst>キーボードショートカット</Kst>
        - <kbd>D</kbd> **Distance:** カーソルを動かして距離を指定します。
5. 右クリックで確定します。

<VideoPlayer fileName="refillet-face"/>

## 関連項目
- [Fillet](fillet)
- [Remove Fillets from Shell](remove-fillets-from-shell)