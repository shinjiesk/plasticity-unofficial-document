import Reference from 'components/Reference'

# Rebuild（フェイスを再構築する）

<Reference shortcut="" command="Rebuild" button="Right of Command Bar" />

このコマンドは、選択されていたオブジェクトに応じて以下のコマンドを自動的に呼び出します。
    - カーブが選択されているとき: スケッチコマンドの[**Rebuild Curve**](../sketch/rebuild#rebuild-curve) を参照してください。
    - フェースが選択されているとき: 以下の **Rebuild Face** を参照してください。

## Rebuild Face [#rebuild-face]

このコマンドは現在開発中です。



