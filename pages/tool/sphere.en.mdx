import Reference from '/components/Reference'
import ButtonIcon from '/components/ButtonIcon';
import VideoPlayer from '/components/VideoPlayer';
import Kst from '/components/KeyboardShortcutsTitle'

# Sphere

<Reference shortcut='' command='Sphere' button='Toolbar' buttonIcon='button-sphere'/>

You can create a sphere by specifying its center and diameter.

## Basic Usage

1. Click the <ButtonIcon type="button-sphere" /> button on the toolbar.
2. Click to set the center of the sphere.
3. Move the cursor to adjust the size of the sphere, and click to confirm.
   - <Kst>Command Dialog</Kst>
      - **Select target bodies:**
            - Functions the same as the Boolean command. Refer to [**Boolean**](/solid/boolean) command for details.
      - **Radius:** Specify the diameter.
   - <Kst>Keyboard Shortcuts</Kst>
      - <kbd>Tab</kbd>: Enables direct input of dimensions. See [here](/tool/sketching-essentials#specify-dimensions-numerically) for details.
      - <kbd>Shift + Q</kbd>, <kbd>Q</kbd>, <kbd>W</kbd>, <kbd>Shift + E</kbd>, <kbd>B</kbd>, <kbd>T</kbd>:
         - Functions the same as the Boolean command. Refer to [**Boolean**](/solid/boolean) command for details.
      - <kbd>D</kbd> **Radius:** Adjust the diameter by moving the cursor.
      - <kbd>X</kbd>/ <kbd>Y</kbd>/<kbd>Z</kbd> **X/Y/Z-axis:** [Constrains to X, Y, Z axis](/tool/sketching-essentials#constrain-to-an-axis).
4. You can also adjust the shape by moving the yellow dot.
5. Click OK in the command dialog or right-click to confirm.
6. The sphere is created.


<VideoPlayer fileName="sphere"/>