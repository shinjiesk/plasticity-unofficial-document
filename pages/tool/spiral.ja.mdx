import VideoPlayer from '/components/VideoPlayer';
import Reference from '/components/Reference'
import ButtonIcon from '/components/ButtonIcon';
import Kst from '/components/KeyboardShortcutsTitle'

# Spiral（螺旋）

<Reference shortcut='' command='Spiral' button='ツールバー' buttonIcon='button-spiral' />

直径と回転数を指定して螺旋を作成できます。

## 基本的な使い方

1. ツールバーのボタン <ButtonIcon type="button-spiral" /> をクリックします。
2. 螺旋の軸の始点でクリックします。
3. カーソルを動かして螺旋の軸の終点でクリックします。
   - <Kst>キーボードショートカット</Kst>
      - <kbd>X</kbd>/ <kbd>Y</kbd>/<kbd>Z</kbd> **X/Y/Z-axis:** [X、Y、Z軸に拘束します](/tool/sketching-essentials#constrain-to-an-axis)。
4. カーソルを動かして螺旋の直径を調整し、クリックして確定します。
5. コマンドダイアログとキーボードショートカットでオプションを設定します。
   - <Kst>コマンドダイアログ</Kst>
      - **Turns:** 回転させる数を指定します。       
      - **Radius:** 直径を指定します。               
      - **Angle OR Pitch:** 角度またはピッチを指定します。それぞれの値が連動します。 
      - **Handedness:** 回転方向を反転させます。 
   - <Kst>キーボードショートカット</Kst>
      - <kbd>A</kbd> **Angle:** 角度をマウス移動で指定します。
      - <kbd>D</kbd> **Length:** 軸の長さをマウス移動で指定します。
      - <kbd>R</kbd> **Radius:** 直径をマウス移動で指定します。それぞれの値が連動します。
4. 黄色のドットと白い円を動かして形状を調整することもできます。
6. コマンドダイアログのOKをクリックするか、右クリックで確定します。
7. 螺旋が作成されます。

<VideoPlayer fileName="spiral"/>



