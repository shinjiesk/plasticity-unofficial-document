import VideoPlayer from '/components/VideoPlayer';

# スケッチの基本

## 正確にスナップする[#accurate-snap]

- カーブをサーフェスモデリングに使うときは、正確にスナップする必要があります。
- 頂点がカーブや他の頂点に正確にスナップされると、紫色から青緑色に変わります。スナップされていない場合は、紫色のままです。
![Sketching Tips 1](/img/sketching-tips-1.png)
- 正確にスナップするには
    - 頂点を動かしてスナップするとき: <kbd>Ctrl</kbd>を押しながらドラッグします。
    - スケッチで描いているとき: **Object snapping** が有効なときは、<kbd>Ctrl</kbd>を押さなくてもスナップできます。
    - 詳しくは[グリッドとスナップ](/plasticity-essentials/grid-and-snap)を参照してください。

## 補助線にスナップする[#snap-reference-lines]

- スケッチを描いているとき、補助線にスナップできます。オブジェクトにカーソルを合わせてShiftキーを押すと補助線が表示されます。

    <VideoPlayer fileName="sketching-tips-2"/>

## 寸法を数値で指定する[#specify-dimensions-numerically]

- スケッチの寸法や角度を数値で指定できます。Tabキーを押すと、寸法値の上に数値入力ボックスが表示されます。

    <VideoPlayer fileName="sketching-tips-3"/>

## X、Y、Z軸に拘束する[#constrain-to-an-axis]

カーソルの移動を座標軸方向に拘束できます。

<VideoPlayer fileName="sketching-tips-4"/>

## ナイフモードを使う[#use-knife-mode]

- ナイフモードを使うと、カーブを描きながら1つまたは複数の面をカットできます。描画中にターゲット面のポイントにスナップする必要があります。

<VideoPlayer fileName="sketching-tips-5"/>