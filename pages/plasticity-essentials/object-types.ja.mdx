# オブジェクトの種類

オブジェクトは「スケッチ」「ソリッド」「エンプティ」に分類され、その中にさらに細かく分類されます。

    1. [スケッチ](#sketch)
        1. [カーブ](#curve)
        2. [頂点](#vertex)
        3. [制御点](#control-vertex)
        4. [リージョン](#region)
    2. [ソリッド](#solid)
        1. [ソリッド](#solid-1)
        2. [シート](#sheet)
        3. [フェイス](#face)
        4. [エッジ](#edge)
    3. [エンプティ](#empty)
        1. [画像](#image)
        2. [メッシュ](#mesh)

## 1. スケッチ[#sketch]

### 1. カーブ（Curve）[#curve]
    - 頂点と制御点から成る、直線または曲線をカーブと呼びます。
    - 選択するには、[**エッジ選択モード**](selection-mode)にします。
    - 複数のカーブを接続し、ひとつのカーブとして扱うこともできます。
    - 青い線で表示され、選択すると黄色になります。

![Curve](/img/object-name-curve.png)

### 2. 頂点（Vertex）[#vertex]
- カーブの両端の点を頂点と呼びます。
- 選択するには、[**コントロールポイント選択モード**](selection-mode)にします。
- 紫色の点で表示され、選択すると白になります。
- 複数の頂点の位置が一致すると点が青緑色になります。

![Vertex](/img/object-name-vertex.png)

### 3. 制御点（Control-Vertex）[#control-vertex]
- カーブの形状を定義する点を制御点と呼びます。
- 選択するには、[**コントロールポイント選択モード**](selection-mode)にします。
- 頂点よりも小さな紫色の点で表示され、選択すると白になります。
- 頂点と各制御点は白い線で結ばれています。
- 制御点を追加することで、より複雑な曲線を表現できます。

![Control-Vertex](/img/object-name-cv.png)

### 4. リージョン（Region）[#region]
- カーブで囲まれた領域をリージョンと呼びます。
- 選択するには、[**フェイス選択モード**](selection-mode)にします。
- 濃い青の面で表示され、選択すると水色になります。
- リージョンを選択すると、自動的に[**Extrude**](/solid/extrude)コマンドが起動します。

## 2. ソリッド[#solid]

### 1. ソリッド（Solid）[#solid-1]
- 複数のフェイスから成る、穴の空いていない閉じた立体をソリッドと呼びます。
- 選択するには、[**ボディ選択モード**](selection-mode)にします。
- ソリッドのフェイスの一部を削除すると開いた立体となり、シートになります。
- 灰色の立体で表示され、選択するとアウトラインが黄色になります。

![Solid](/img/object-name-solid.png)

### 2. シート（Sheet）[#sheet]
- フェイスから成る、穴の空いた開いた立体をシートを呼びます。
- 選択するには、[**ボディ選択モード**](selection-mode)にします。
- 灰色の立体で表示され、選択するとアウトラインが黄色になります。

![Sheet](/img/object-name-sheet.png)

### 3. フェイス（Face）[#face]
- ソリッドとシートを構成するそれぞれの平面または曲面をフェイスと呼びます。
- 選択するには、[**フェイス選択モード**](selection-mode)にします。
- 灰色で表示され、選択すると薄い黄色になります。
- フェイスを選択すると、自動的に [**Offset Face**](/solid/offset-face) コマンドが起動します。

![Face](/img/object-name-face.png)

### 4. エッジ（Edge）[#edge]
- フェイスを取り囲む境界線をエッジと呼びます。
- 選択するには、[**エッジ選択モード**](selection-mode)にします。
- 黒色の線で表示され、選択すると黄色になります。
- ふたつのフェイスが接するエッジを選択したときは、自動的に [**Fillet**](/solid/fillet) コマンドが起動します。また、シートの開いたエッジを選択したときは、自動的に [**Extend Sheet**](/solid/extend-sheet)コマンドが起動します。

![Edge](/img/object-name-edge.png)

## 3. エンプティ（Empty）[#empty]

### 1. イメージ（Image）[#image]
- Import機能で読み込んだ画像をイメージと呼びます。
- 選択するには、[**ボディ選択モード**](selection-mode)にします。
- 選択するとアウトラインが黄色になります。

### 2. メッシュ（Mesh）[#mesh]
- Import機能で読み込んだSTLやFBXなどの3Dデータをメッシュと呼びます。
- 選択するには、[**ボディ選択モード**](selection-mode)にします。
- 選択するとアウトラインが黄色になります。

