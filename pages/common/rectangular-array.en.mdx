import Reference from '/components/Reference'
import ButtonIcon from '/components/ButtonIcon';
import Kst from '/components/KeyboardShortcutsTitle'

# Rectangular Array

<Reference shortcut="" command="Rectangular Array" button="Right of Command Bar"  buttonIcon='button-rectangular-array'/>

Duplicate objects in a linear pattern by specifying the number of duplicates. Optionally, define another direction to create a rectangular array.

## Basic Usage

1. Select the object you want to duplicate.
2. Click the button <ButtonIcon type="button-rectangular-array" /> on the Right of Command Bar.
3. The duplication preview will be displayed.
   - <kbd>X</kbd>/ <kbd>Y</kbd>/<kbd>Z</kbd> **X/Y/Z-axis:** [Constrain to the X, Y, or Z-axis](/tool/sketching-essentials#constrain-to-an-axis).
4. Click on the desired direction.
5. Set the options in the Command Dialog and Keyboard Shortcuts.
   - <Kst>Command Dialog</Kst>
      - **Distance 1:** Specify the distance.
      - **Number:** Specify the number of duplications.
   - <Kst>Keyboard Shortcuts</Kst>
   - <kbd>Shift + Wheel up</kbd> **Add copy:** Increases the number of duplications.
   - <kbd>Shift + Wheel down</kbd> **Subtract copy:** Reduces the number of duplications.
6. If you want to duplicate in another direction, click on **Select endpoint 2** in the dialog.
   1. Right-click on the desired direction for duplication.
   2. Set the options in the Command Dialog 
      - <Kst>Command Dialog</Kst>
         - **Distance 2:** Specify the distance.
         - **Repeat:** Specify the number of repetitions.
      - <Kst>Keyboard Shortcuts</Kst>
      - <kbd>Shift + Wheel up</kbd> **Add copy:** Increases the number of duplications.
      - <kbd>Shift + Wheel down</kbd> **Subtract copy:** Reduces the number of duplications.
7. Confirm with OK in the Command Dialog or right-click.

## See also
- [Curve Array](curve-array)
- [Radial Array](radial-array)