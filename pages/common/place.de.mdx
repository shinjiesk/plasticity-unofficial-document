import VideoPlayer from '/components/VideoPlayer';
import Reference from '/components/Reference'
import Kst from '/components/KeyboardShortcutsTitle'

# Place

<Reference shortcut="Strg + D" command="Place" button="Links von der Befehlsleiste"  buttonIcon='button-place'/>

Duplizieren Sie Objekte und platzieren Sie sie auf dem Referenzpunkt. Erlaubt Skalierung, Rotation und das Spiegeln der Achse. Es kann für boolesche Operationen verwendet werden.

## Grundlegende Nutzung

1. Wählen Sie die Objekte aus, die Sie duplizieren möchten.
2. Drücken Sie <kbd>Strg + D</kbd> oder klicken Sie auf die Schaltfläche.
3. Klicken Sie auf den Referenzpunkt des zu duplizierenden Objekts.
4. Klicken Sie auf den Referenzpunkt des Ziels für die Duplikation.
   - <Kst>Tastenkombinationen</Kst>
      - <kbd>S</kbd> **Scale:** Erlaubt das Skalieren.
      - <kbd>A</kbd> **Angle:** Erlaubt das Drehen.
      - <kbd>D</kbd> **Offset:** Erlaubt aufeinanderfolgende Duplikation.
      - <kbd>F</kbd> **Flip:** Erlaubt das Spiegeln der Duplikationsachse.
      - <kbd>X</kbd>/<kbd>Y</kbd>/<kbd>Z</kbd> **x/y/z-up:** Legt die Duplikationsachse auf die X/Y/Z-Achse fest.
      - <kbd>Umschalt + Q</kbd> **Slice:** Erstellt einen Solid an den Schnittstellen mit dem Boolean-Ziel und bewahrt die verbleibenden Solids.
      - <kbd>Q</kbd> **Union:** Verbindet das Boolean-Ziel und das Werkzeug.
      - <kbd>W</kbd> **Difference:** Entfernt das Werkzeug vom Boolean-Ziel.
      - <kbd>Umschalt + E</kbd> **Intersect:** Erstellt einen Solid an der Schnittstelle des Boolean-Ziels und des Werkzeugs.
      - <kbd>B</kbd> **New Body:** Erstellt einen neuen Solid ohne Boolean-Operationen auszuführen.
5. Klicken Sie zur Bestätigung.
6. Um weitere Objekte zu duplizieren, wiederholen Sie die Schritte 4 und 5.
7. Drücken Sie <kbd>Esc</kbd>, um den Befehl zu beenden.

<VideoPlayer fileName="place"/>

## Siehe auch
- [Duplicate](duplicate)