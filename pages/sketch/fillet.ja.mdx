import Reference from 'components/Reference'
import ButtonIcon from 'components/ButtonIcon';

# Fillet（フィレット）

<Reference shortcut="B" command="Fillet" button="コマンドバー右" />

このコマンドは、選択されたオブジェクトに応じて、以下のコマンドを自動的に呼び出します。
  - カーブまたは頂点が選択されているとき: 下記の **"カーブと頂点をフィレットする"** を参照してください。
  - エッジが選択されているとき: ソリッドコマンドの [**Fillet**](../solid/fillet) を参照してください。

## カーブと頂点をフィレットする

カーブとカーブの角の頂点を、フィレットまたは面取りできます。

1. フィレットまたは面取りをしたいカーブまたは頂点を選択します。
2. <kbd>B</kbd> を押すか、コマンドバー右の <ButtonIcon type="button-fillet" /> をクリックします。
3. 黄色い点を動かすことで、フィレットまたは面取りを作成できます。動かす方向によって、フィレットまたは面取りが作成されます。
4. コマンドダイアログとキーボードショートカットでオプションを設定します。
  - **Distance:** 距離を指定します。
   - <kbd>D</kbd> **Fillet Distance:** カーソルを動かすことで、フィレットの距離を指定します。
   - <kbd>C</kbd> **Chamfer Distance:** カーソルを動かすことで、面取りの距離を指定します。
5. コマンドダイアログの OK をクリックするか、右クリックで確定します。

![Fillet](/img/fillet.gif)





