import Reference from 'components/Reference';

<Reference shortcut="" command="Delete Redundant Topology" button="" />

# Delete Redundant Topology（余剰トポロジーの削除）

このコマンドは、選択されていたオブジェクトに応じて以下のコマンドを自動的に呼び出します。
- カーブが選択されているとき: [**Delete Redundant Topology (Curves)**](../sketch/delete-redundant-Topology#delete-redundant-Topology-curves) を参照してください。
- ソリッドまたはシートが選択されているとき: 以下の [**Delete Redundant Topology (Solid)**](../solid/delete-redundant-Topology#delete-redundant-Topology-solid) を参照してください。

## Delete Redundant Topology (Curves)[#delete-redundant-Topology-curves]

カーブの余剰な制御点を削除することができます。

1. 余剰な制御点を削除するカーブを選択します。
2. コマンドパレットで **Delete Redundant Topology** を実行します。
3. 余剰な制御点が削除されます。

![Delete Redundant Topology](/img/delete-redundant-topology.gif)