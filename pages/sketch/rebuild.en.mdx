import Reference from '/components/Reference'

# Rebuild

<Reference shortcut="" command="Rebuild" button="Right of Command Bar" buttonIcon='button-rebuild' />

Unified command for **Rebuild Curve**, **Rebuild Face**. The command is run based on the selected object.
  - When Curves are selected: see [**Rebuild Curve**](rebuild-curve).
  - When Faces are selected: **Rebuild Face** (Currently under development).

## See also
- [Rebuild Curve](rebuild-curve)
- [Delete Redundant Topology](delete-redundant-topology)