import Reference from '/components/Reference'
import { Callout } from 'nextra/components'

# Join

<Reference shortcut="J" command="Join" button="" />

このコマンドは、選択されていたオブジェクトに応じて以下のコマンドを自動的に呼び出します。
    - カーブが選択されているとき: 下記の**Join Curves** を参照してください。
    - シートが選択されているとき: ソリッドコマンドの [**Join Sheets**](/solid/join#join-sheets)を参照してください。

## 関連項目
- [Join Curves](join-curves)
- [Bridge](bridge)