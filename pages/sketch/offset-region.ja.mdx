import VideoPlayer from '/components/VideoPlayer';
import Reference from '/components/Reference'
import Kst from '/components/KeyboardShortcutsTitle'

# Offset Region（リージョンをオフセットする）[#offset-region]

<Reference shortcut="O" command="Offset Region" button="コマンドバー右"  buttonIcon='button-offset-curve'/>

選択したリージョンのアウトラインカーブを距離を指定してオフセットし、新しいリージョンを作成します。複数のリージョンを選択すると、新しい個々のリージョンまたは結合されたリージョンが作成されます。

## 基本的な使い方

1. オフセットしたいリージョンを選択します。
2. <kbd>O</kbd>を押します。
3. 黄色い矢印を動かして距離を指定します。
4. コマンドダイアログとキーボードショートカットでオプションを設定します。
   - <Kst>コマンドダイアログ</Kst>
      - **Distance:** 距離を指定します。
      - **Gap fill:**
         - **Round:** 出っ張った角を丸めます。
         - **Linear:** 出っ張った角を直線でつなぎます。 
         - **Natural** ナチュラル
   - <Kst>キーボードショートカット</Kst>
      - <kbd>Tab</kbd> **Lock distances:** 両側に等しい距離でオフセットします。
      - <kbd>I</kbd> **Individual:** 複数のリージョンが選択されているとき、このオプションをオンにすると、リージョンを結合してオフセットします。
      - <kbd>V</kbd> **Gap fill:**
      - <kbd>D</kbd> **Distance:** マウスの動きで距離を指定します。
5. コマンドダイアログのOKか右クリックで確定します。

<VideoPlayer fileName="offset-region"/>

## 関連項目
- [Offset Curve](offset-curve)
- [Offset Vertex](offset-vertex)
- [Offset Planar Curve](offset-planar-curve)
