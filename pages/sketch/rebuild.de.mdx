import Reference from '/components/Reference'

# Rebuild

<Reference shortcut="" command="Rebuild" button="Rechts von der Befehlsleiste" buttonIcon='button-rebuild'/>

Einheitlicher Befehl für **Rebuild Curve**, **Rebuild Face**. Der Befehl wird basierend auf dem ausgewählten Objekt ausgeführt.
  - Wenn Curves ausgewählt sind: siehe [**Rebuild Curve**](rebuild-curve).
  - Wenn Faces ausgewählt sind: **Rebuild Face** (Derzeit in Entwicklung).

## See also
- [Rebuild Curve](rebuild-curve)
- [Delete Redundant Topology](delete-redundant-topology)