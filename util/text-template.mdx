import Reference from '/components/Reference'
import ButtonIcon from '/components/ButtonIcon'

# Dummy Page

<Reference shortcut="" command="Curve Array" button="Right of Command Bar" />

This command automatically calls the following command depending on the selected object.
- When Curves are selected: see [**Offset Planar Curve**](#offset-planar-curve) described below.
- When Faces are selected: see [**Offset Face Loop**](/solid/offset-curve#offset-face-loop) of **Offset Curve** in the Solid Commands section.

自由に拡大縮小するには、下記の [**Freestyle Scale**](#freestyle-scale) を参照してください。

You can 

1. Select the object you want to duplicate.

2. Press <kbd></kbd>.
2. <kbd>R</kbd>を押します。

2. Use **""** in the Command Palette.
2. コマンドパレットで**""**を実行します。

2. Click the button <ButtonIcon type="button-radial-array" /> on the Right of Command Bar.
2. Press <kbd></kbd> or click the button <ButtonIcon type="button-" /> on the Right of Command Bar.
2. When a Face is selected, the default command, Offset Face, is automatically executed.

3. Proceed to step 4.

3. Select the  Curve to use as the path.
3. You can create a fillet or chamfer by moving the yellow point.
3. Move the yellow dot to specify the distance.

Set the options in the Command Dialog and Keyboard Shortcuts.

コマンドダイアログとキーボードショートカットでオプションを設定します。
コマンドダイアログでオプションを設定します。
キーボードショートカットでオプションを設定します。

   - **** Specify the 
   - <kbd></kbd> **** 

   - <kbd></kbd> **** Specify the   with mouse movement.

   - <kbd>Shift</kbd> + Wheel up **Add copy** Increases the number of duplications.
   - <kbd>Shift</kbd> + Wheel down **Subtract copy** Reduces the number of duplications.

   - <kbd>Tab</kbd> **Lock distances** Extrude with equal distance on both sides.

   - <kbd>F</kbd> **Freestyle** It functions similarly to the **Freestyle Offset Planar Curve** described below.

   - <kbd>G</kbd> **Screen space:** Limit to the visible plane.
   - <kbd>V</kbd> **Pivot:** Click to specify the position of the Gizmo.
   - <kbd>W</kbd> **Worldspace:** Remove restrictions.pnpm



   - <kbd>F</kbd> **Freestyle:** Click to specify the starting point and endpoint, which will determine the direction and distance of movement.

   - <kbd>G</kbd> **Screen space:** 見た目の平面に限定します。
   - <kbd>V</kbd> **Pivot:** クリックしてギズモの位置を指定します。
   - <kbd>W</kbd> **Worldspace:** 限定を解除します。

      - **Select target bodies:** ブーリアンのターゲットにするソリッドやシートを選択します。
         - <kbd>Shift + Q</kbd> **Slice:** ブーリアン対象との交点にソリッドを作成し、余りのソリッドも残ります。
         - <kbd>Q</kbd> **Union:** ブーリアン対象と結合します。
         - <kbd>W</kbd> **Difference:** ブーリアン対象から切り取ります。
         - <kbd>Shift + E</kbd> **Intersect:** ブーリアン対象との交点にソリッドを作成します。
         - <kbd>B</kbd> **New Body:** 新規にソリッドを作成します。ブーリアンは実行されません。
         - <kbd>T</kbd> **Keep Tool:** ブーリアンが実行され、ツールのオブジェクトはそのまま残ります。

   - <kbd>F</kbd> **Freestyle:** 1回目のクリックを始点、2回目のクリックを終点として、移動の方向と距離を指定します。

   - **Drag the yellow circle**
        - Specify the section size.
   - **Drag the yellow square**
        - Specify the pipe thickness.
   - **Drag the white circle**
        - Specify the rotation angle.


6. If you need to specify Sweep guides.

Confirm with OK in the Command Dialog or right-click.

コマンドダイアログのOKをクリックするか、右クリックで確定します。

5. A circle is created.

![](/img/hoge.gif)

3. Move the cursor and click at the endpoint of the spiral axis.

By moving the cursor, you can specify the

This command can be executed not only through the **Freestyle** option in the **Offset Curve** command but also by using the **Freestyle Offset Planar Curve** in the Command Palette.

3. Press <kbd>F</kbd> to enter Freestyle mode.
4. Move the cursor to the offset point and click.
   - If Grid Snaps are enabled, the offset point snaps to the grid.
   - If Object Snaps are enabled, the offset point snaps to the object.





import { Callout } from 'nextra/components'


<Callout type="info" emoji="ℹ️">
**Advanced info**<br />
</Callout>

<Callout type="info" emoji="💡">
</Callout>


<Callout emoji="❗">
**Note**<br />
</Callout>

<Callout type="warning" emoji="⚠️">
</Callout>

**高度な情報**<br />
このコマンドは、選択されていたオブジェクトに応じて以下のコマンドを自動的に呼び出します。




## 関連項目
